;;; package -- Summery
;;; Commentary:

;;; Code:
(global-linum-mode 1)
(desktop-save-mode 0)

(custom-set-variables
 '(initial-frame-alist (quote ((fullscreen . maximized)))))

(provide 'init-local)
;;; init-local.el ends here
